function getArea(length,width) {
    let area = (length * width) ;
    return area;
}
console.log(getArea());

function getPerimeter(length,width) {
    let perimeter = (length + width + length + width);

    return perimeter;
}
console.log(getPerimeter());